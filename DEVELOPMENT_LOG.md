# S3Marks 开发日志

> 按你的要求，这份文档用于逐项追踪功能实现进度。

## 进度

| 任务 | 功能 | 状态 | 说明 |
| --- | --- | --- | --- |
| 1 | 项目初始化 | 已完成 | 已创建 Vite、React、TypeScript、Manifest V3、popup/options/background 入口和图标资源。构建验证记录在最终验证步骤中。 |
| 2 | 读取书签 | 已完成 | 已实现 Bookmarks API 封装、标准化书签树转换、书签计数和 popup 展示。标准化结果会输出到 console。 |
| 3 | S3 配置页 | 已完成 | Options 页面支持从本地扩展存储保存和回显 S3 配置，支持连接测试、清空本地同步状态和查看日志。 |
| 4 | 上传书签到 S3 | 已完成 | 本地标准化书签会序列化为 `latest.json` 或 `latest.json.enc`，并上传 metadata 和 history 版本到 S3。 |
| 5 | 从 S3 下载书签 | 已完成 | 可下载最新远程同步文件，按需解密、解析并统计远程书签数量。 |
| 6 | 导入到浏览器 | 已完成 | 下载或合并后的书签树现在会写入浏览器原生根目录，例如 `Bookmarks Bar` / `Other Bookmarks`；`S3Marks` 不再作为保留托管目录名处理。 |
| 7 | 手动同步 | 已完成 | popup 现在只保留“立即同步”主按钮，并在 UI 中展示状态和错误；上传/下载能力保留在后台流程中用于初始化和恢复。 |
| 8 | 基础三方合并 | 已完成 | Base/Local/Remote 合并会保留非冲突新增内容，并把冲突写入 `Sync Conflicts` 文件夹。 |
| 9 | 加密 | 已完成 | 上传前使用 PBKDF2/SHA-256 和 AES-GCM 加密，下载时按 metadata 指向的最新对象按需解密，支持后续切换加密或不加密。 |
| 10 | 自动同步 | 已完成 | Options 保存后会按 15/30/60 分钟配置重排 `chrome.alarms` 定时器，后台闹钟会执行 `syncNow`。 |

## 记录

- 2026-06-02：完成任务 1 项目初始化。创建 Vite + React + TypeScript + Manifest V3 插件结构，包括 popup、options、background 入口、图标资源和这份追踪日志。
- 2026-06-02：完成任务 2 读取书签。添加跨浏览器 Bookmarks API 封装、带稳定路径和 index 的标准化逻辑、popup 书签计数，以及标准化书签树 console 输出。
- 2026-06-02：开始任务 6 导入到浏览器。添加安全导入器，只清空并重建受管理的 `S3Marks` 根目录，不影响浏览器原有书签区域。
- 2026-06-02：完成任务 3 S3 配置页。添加基于 `chrome.storage.local` 的配置持久化、S3 连接测试、加密和自动同步字段、本地状态清空，以及 Options 页面同步日志查看。
- 2026-06-02：完成任务 4 上传书签到 S3。添加同步文件生成、`latest.json` 或 `latest.json.enc` 上传、`metadata.json` 更新和 revision history 上传。
- 2026-06-02：完成任务 5 从 S3 下载书签。添加最新文件发现、JSON 解析、加密 payload 检测和远程书签统计。
- 2026-06-02：完成任务 6 导入到浏览器。将下载和合并后的书签树接入安全的 `S3Marks` 受管理根目录导入器。
- 2026-06-02：完成任务 7 手动同步。添加 popup 控件，用于通过 background service worker 执行立即同步、仅上传和仅下载流程。
- 2026-06-02：完成任务 8 基础三方合并。添加 Base/Local/Remote 合并逻辑，处理新增和删除与修改冲突，并把冲突副本放入 `Sync Conflicts/<timestamp>/Local` 和 `Remote`。
- 2026-06-02：完成任务 9 加密。添加 PBKDF2/SHA-256 密钥派生和 AES-GCM JSON 加密/解密，用于远程同步文件。
- 2026-06-02：完成任务 10 自动同步。添加根据保存间隔创建后台 alarm、Options 保存后重排定时器，以及周期性执行 `syncNow`。
- 2026-06-02：完成验证。已运行 `npm install` 和 `npm run build`；TypeScript 与 Vite 构建通过，且 `dist/manifest.json` 引用的 popup、options、background 和图标文件都存在。
- 2026-06-02：完成最终验证。UI 运行时访问方式加固后，重新运行 `npm run build`；TypeScript 与 Vite 构建通过。
- 2026-06-02：修复受管理目录重复同步问题。现在读取本地书签时会在任意层级排除 `S3Marks` 文件夹，并在下载远程同步文件后清洗已被污染的嵌套 `S3Marks` 数据，避免出现 `S3Marks/S3Marks/...` 目录递归。
- 2026-06-02：完成重复目录修复验证。重新运行 `npm run build`；TypeScript 与 Vite 构建通过。
- 2026-06-02：简化 popup 操作入口。根据使用反馈移除公开的“上传本地到 S3”和“从 S3 下载”按钮，只保留“立即同步”和“设置”，让日常使用路径更明确。
- 2026-06-02：完成 popup 简化验证。重新运行 `npm run build`；TypeScript 与 Vite 构建通过。
- 2026-06-02：优化首次同步体验。现在用户首次安装后只需点击“立即同步”：如果远程为空，会用当前浏览器初始化 S3；如果远程已有数据且当前浏览器没有同步基线，会自动从 S3 导入并建立基线。
- 2026-06-02：完成首次同步体验验证。重新运行 `npm run build`；TypeScript 与 Vite 构建通过。
- 2026-06-02：开始修复同步结果偶发不一致问题。定位到本地读取范围和 `S3Marks` 托管目录可能不一致，以及 Edge/Chrome 中英文根目录名没有完全归一导致结构重复。
- 2026-06-02：完成同步稳定性修复。本地同步读取现在会合并普通浏览器书签和 `S3Marks` 托管目录内容，避免导入后的远程内容在下一轮同步中被误判为删除；同时归一化 Edge/Chrome 的 `收藏夹栏`、`其他收藏夹`、`Favorites Bar`、`Other Favorites` 等根目录名称。
- 2026-06-02：新增同步场景测试。添加 `npm run test:sync`，覆盖 Chrome/Edge 首次导入后再次同步不丢内容，以及中英文根目录合并场景。
- 2026-06-02：完成同步稳定性验证。已运行 `npm run test:sync` 和 `npm run build`；同步场景测试、TypeScript 与 Vite 构建均通过。
- 2026-06-02：修复旧基线迁移问题。真实检查 Chrome/Edge 本地扩展状态后发现旧 `lastSyncState` 中仍有 `收藏夹栏` 等中文根目录路径，参与合并时会把远程新增误判成删除；现在合并前会先规范化本地基线。
- 2026-06-02：完成旧基线迁移验证。新增“旧中文基线 + 新英文远程”测试场景，并重新运行 `npm run test:sync` 与 `npm run build`；全部通过。
- 2026-06-02：开始增强自动同步体验。准备加入配置保存后立即同步、浏览器启动后同步、书签变化防抖同步，以及后台同步锁，避免多个触发源并发覆盖远程。
- 2026-06-02：完成自动同步增强。新增配置保存后同步、浏览器启动后同步、书签变化 15 秒防抖同步、后台同步锁，以及 60 分钟默认定时兜底同步；Options 页面新增“书签变化后自动同步”和“浏览器启动后自动同步”开关。
- 2026-06-02：完成自动同步增强验证。重新运行 `npm run test:sync` 和 `npm run build`；同步场景测试、TypeScript 与 Vite 构建均通过。
- 2026-06-02：开始修复原生收藏夹和 `S3Marks` 目录双份数据问题。根据真实截图确认旧“受管理目录导入”会导致同一书签同时出现在收藏栏和 `其他收藏夹/S3Marks/Bookmarks Bar` 下，删除时需要删两处，体验不符合直接同步预期。
- 2026-06-02：完成原生根目录写回改造。同步结果现在直接写入浏览器自己的收藏栏、其他收藏夹等根目录；写入前会迁移旧 `S3Marks` 内容并删除旧目录，不再创建新的 `S3Marks` 托管根目录。
- 2026-06-02：优化首次同步策略。远程已有数据且本地没有同步基线时，现在会先合并本地书签和远程书签，再写回浏览器和上传新 revision，避免直接导入远程时覆盖新浏览器本地已有收藏。
- 2026-06-02：新增原生目录迁移测试。覆盖旧 `S3Marks` 内容可迁移到 `Bookmarks Bar`、写入计划不再创建 `S3Marks` 根目录，以及异常顶层目录会归入 `Other Bookmarks`。
- 2026-06-02：完成原生目录写回验证。重新运行 `npm run test:sync` 和 `npm run build`；同步场景测试、TypeScript 与 Vite 构建均通过，`dist` 已更新为不再创建 `S3Marks` 托管目录的版本。
- 2026-06-02：开始修复加密模式切换问题。确认旧逻辑会优先读取 `latest.json.enc`，导致从加密切回不加密后可能继续读旧加密文件或报缺少密码。
- 2026-06-02：完成加密模式切换修复。`metadata.json` 现在记录 `latestObjectKey` 和 `latestEncrypted`，下载时优先按 metadata 指向读取；旧 metadata 没有这些字段时，会按 revision 做兼容选择，避免旧 `latest.json.enc` 抢读新明文。
- 2026-06-02：完成旧 latest 清理逻辑。上传加密版本后会尝试删除旧 `latest.json`，上传明文版本后会尝试删除旧 `latest.json.enc`；删除失败不会阻断同步，因为 metadata 已能指向正确文件。
- 2026-06-02：完成加密模式切换验证。重新运行 `npm run test:sync` 和 `npm run build`；同步场景测试、TypeScript 与 Vite 构建均通过。
- 2026-06-02：补充加密切换专项测试。将 latest 对象选择逻辑拆为可测试纯函数，并覆盖 metadata 指向明文、metadata 指向加密、旧 metadata 按 revision 选择、无 metadata 按最高 revision 兜底等场景；重新运行 `npm run test:sync` 和 `npm run build`，全部通过。
- 2026-06-02：完成 README 中文详细化。将 README 从英文简要说明改为中文使用文档，补充安装构建、浏览器加载、首次使用、S3/COS 配置、同步文件结构、加密切换、自动同步、安全说明和常见问题。
- 2026-06-02：开始准备插件市场上架资料。根据 Chrome Web Store 和 Microsoft Edge Add-ons 发布流程，准备商店文案、隐私政策、权限说明、审核测试说明、发布检查清单和 zip 打包脚本。
- 2026-06-02：完成上架资料整理。新增 `store/` 资料目录，收窄 host permission 为 `https://*/*`，并添加 `npm run package:extension` 用于生成插件市场上传包。
- 2026-06-02：完成上架包验证。重新运行 `npm run test:sync`、`npm run build` 和 `npm run package:extension`；生成 `release/s3marks-v0.1.0.zip`，并确认 zip 顶层包含 `manifest.json`。
- 2026-06-02：调整发布方案为 GitHub Release。移除未计划使用的插件市场资料，保留 zip 打包脚本和 `release/` 忽略规则，并在 README 中改为说明 GitHub Release 下载、解压和开发者模式加载流程。
- 2026-06-02：移除旧托管目录兼容逻辑。`S3Marks` 不再按标题特殊迁移、排除或删除，用户创建的同名文件夹会作为普通书签文件夹参与同步；同步场景测试已改为覆盖该行为。
- 2026-06-02：修复同步期间本地新增可能被旧快照覆盖的问题。后台现在只忽略插件写回书签时产生的事件，同步中发生的用户书签变化会排队再同步；写回浏览器前会重新读取本地书签，若检测到本地变化则用最新本地树重新合并。
- 2026-06-02：修复两个高风险同步问题。浏览器原生根目录写回前会备份将要修改的根目录，写入失败会尝试回滚；远程上传改为先写唯一 history 对象，再用 `metadata.json` ETag 条件更新，避免多设备同时同步时互相覆盖。
